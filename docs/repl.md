# REPL

Start the interactive prompt:

```sh
cherry repl
```

Expressions print their value automatically:

```cherry
import math
math.sqrt(81)
vec2(3, 4).length()
```

Use `:quit` or `:exit` to leave.
